# Tauri build output
/target/

# Bundled Next.js server (generated by prepare-tauri-bundle.js)
# This folder is produced at build time — never commit it
nextjs/

# Bundled node binary (platform-specific)
binaries/
